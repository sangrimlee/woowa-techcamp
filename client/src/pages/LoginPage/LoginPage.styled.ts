import styled from 'styled-components';

export const TestUser = styled.div`
  position: absolute;
  bottom: 5rem;
  right: 5rem;
  padding: 2.5rem 4rem;
  border-radius: 0.25rem;
  background-color: #fff740;

  & > h3 {
    font-size: 1.55rem;
    font-weight: 700;
    margin-bottom: 1rem;
  }
  & > p {
    font-size: 1.25rem;
    font-weight: 500;
    margin-bottom: 0.5rem;
  }
`;
